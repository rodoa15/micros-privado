server:
  port: 8080
 cloud:
    config:
      enabled: true
      uri: http://localhost:9090
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: productos
          uri: lb://productos
          predicates:
          - Path=/productos/**  
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      default-zone: http://localhost:8761/eureka
  instance:
    hostname: localhost
